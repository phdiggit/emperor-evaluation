# AGENTS.md

本仓库是“皇帝综合评价体系 V4”工作区。执行任务时先守边界，再动手。中文是业务事实源；必要的英文锚点只用于执行稳定性。

## 优先级 / Priority

执行顺序：

1. 当前 Issue / PR / 用户任务中的白名单、禁止事项和显式批准；
2. 本 `AGENTS.md`；
3. V4 当前有效文档与配置；
4. 既有实现和生成物。

冲突时以上层规则为准。发现文档冲突必须显式记录，不得由代码、模型输出、V3 数据或生成物静默覆盖。

## V4 基线

- V4 分支必须保留从 V3 冻结点继承的完整 Git 历史，不得改为 orphan 或回退到早期基线。
- V3 与 V4 的运行环境、业务数据、数据库、任务队列和发布链路严格隔离。
- V4 在 shadow 验收通过且另有明确切换授权前，不得替换 V3 生产服务。
- V3 资产只可作为只读回归样本、负面案例和对照，不得直接成为 V4 正式事实或评分。
- 当前阶段以 `HistoricalEpisode` 为第一语义工作单元；`Assertion` 是证据断言，不是默认调度、覆盖和计分单位。
- 当前阶段是文档、领域模型、服务契约和试点门禁定义期。未通过文档 Gate 前，不新增 `scripts/`、`db/`、`tests/`、worker 或生产运行配置。

## 事实源优先级

1. `docs/项目总纲/皇帝综合评价体系评分标准.md`
2. `docs/00-V4项目章程.md`、`docs/项目总纲/总规则.md`
3. V4 领域模型、证据规则、服务契约与不可妥协约束
4. 分项规则
5. `config/*.yml`

生成视图、模型输出、V3 数据和临时补丁不属于事实源。

## 基本工作规则

- 先用仓库事实判断；范围明确时一次完成白名单内的必要步骤。
- 只读诊断保持只读；写入前先锁定最小改动路径和受影响文档。
- 大文件先用 `rg`、`git grep`、标题或定向行号读取；除非整体重写，不全文扫描无关大文件。
- 修改前后核对 `git -c core.quotepath=false status --short` 与 `git diff --name-only`。
- 不覆盖用户已有改动，不默认执行 `stash`、`clean`、`reset --hard`、强制推送或历史改写。
- 清理、归档和删除第一轮默认只给出诊断；任务明确要求的 V4 初始化清理除外。
- 需要复用 V3 资产时，先提取需求和失败样本，再写 V4 契约与验收条件；不得整文件搬运旧 orchestrator、consumer、migration 或 scorer。

## 数据与副作用

- 默认 `report-only`、`offline-first`、`shadow-first`。
- 未经明确授权，不连接或修改 V3 数据库，不执行生产部署，不写正式评分或排名。
- Git 保存规则、配置和契约；未来 PostgreSQL 保存 V4 业务状态；Markdown/JSON 导出只作为只读视图。
- runtime 路径、并发、超时、凭据和部署参数不得写入核心项目配置。
- 真实 `.env`、令牌、口令、DSN 和私有地址不得提交。发现已提交凭据时，先移除工作树中的文件并提示轮换；未经明确授权不改写 Git 历史。
- 模型只能在 `config/model-policy.yml` 允许的场景被调用，且输出必须经过规则或人工门禁。

## GitHub

- 远端读写优先使用已认证工具；写操作前用最少读取确认仓库、分支、文件 SHA 和改动范围。
- 对同一目标只选择一种写入路径，不反复混用多个接口。
- 提交信息应说明业务意图，而不是只写“update files”。
- 未经明确要求不创建 PR、不合并、不改默认分支。
- 推送前必须读回关键文件或比较分支，确认目标分支与内容正确。

## Shell 与编码

- Windows 上优先使用 `pwsh.exe`；需要 POSIX 工具链或 `.sh` 时使用 Git Bash。
- 禁止用 PowerShell inline、管道或 here-string 传递大段中文给 Python、GitHub CLI 或模型任务；改用 UTF-8 文件。
- 中文 Markdown、YAML、JSON/JSONL 使用 UTF-8 no BOM；JSON 使用 `ensure_ascii=False` 和稳定排序/缩进。
- 多关键词搜索优先使用单条 `rg -n "A|B|C" <paths>`。
- 跨步骤复用的临时文件放 `tmp/**` 或仓库外 runtime；`.tmp/**` 仅用于可随时丢弃的单步产物。

## 子 Agent 与批量任务

- 批量任务必须有稳定 `task_code`、输入版本、允许写入范围、期望输出契约和失败恢复方式。
- 子任务不得直接写正式事实、规则判断、分数或生产数据库。
- 结果不得只看退出码；必须检查任务状态、权限分析、输出契约和项目校验结果。
- 默认只允许写 `tmp/**`；源码写入需要显式 `repo-editor` 范围，绕过权限需要用户明确接受风险。
- V4 生产数据链不得依赖“模型写补丁文件—人工复制—再落库”作为主状态机；正式实现应使用结构化请求、Schema 校验和持久化任务结果。

## 文档 Gate 与未来实现

在引入第一行实现代码前，以下文档必须均为 `accepted`：

- 项目章程与总规则；
- 领域模型与事件/证据模型；
- 规则输入类型；
- 状态机与增量失效；
- 覆盖度和试点门禁；
- 智能体边界；
- 四份服务/数据契约。

进入实现阶段后：

- Schema、最小实现、测试和迁移必须在同一职责链中落地；
- 每个写入口必须有数据库级幂等键；
- 正常运行不得以全表删除重建作为默认路径；
- 每项派生结果必须声明输入版本并可追溯；
- 无变化重跑必须产生零业务写入和零模型调用。

## 验证

当前文档阶段至少验证：

- Markdown 相对链接可解析；
- YAML 可解析且无 runtime/凭据字段；
- 文件树与 `docs/README.md` 一致；
- `git diff --check` 通过；
- 工作树无真实 `.env`。

未来涉及代码、Schema 或 CI 时，再增加 focused tests、compileall、迁移 dry-run 和完整性审计；同一 head 的全量测试原则上只运行一次。

## 默认忽略

除非任务明确点名，不主动读取或改写：

```text
tmp/
.tmp/
.cache/
logs/
exports/
.env
.codex/
```

`.codex/config.toml` 由仓库维护，但其高权限配置不覆盖本文件的数据、生产和安全边界。

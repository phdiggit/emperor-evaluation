# AGENTS.md

本仓库是“皇帝综合评价体系 V4”已结算工作区。当前目标是维护五大项评分的规则、证据、确定性结算逻辑和唯一正式结果，不再维护历史采集或服务运行平台。

## 优先级

1. 当前用户任务中的范围、禁止事项和显式批准；
2. 本文件；
3. `docs/项目总纲/皇帝综合评价体系评分标准.md`；
4. 分项规则与 `config/project.yml`；
5. 既有实现和生成物。

发生冲突时显式报告，不得由代码、旧数据或生成视图静默覆盖上位规则。

## 当前事实

- 第一至第五项均已正式结算。
- 独立人物画像采用逐轴结算；C1、C2、C3、C5、M1、M2、M3与M4八轴均已正式结算。画像不构成第六项，不进入综合总榜，不设画像总分或轴内排名；正式入口由`config/project.yml`的`profile_assessment`登记。
- 当前综合评价正式池为184人；其中174人综合计算就绪，10人待补第二项正式结算；第一项适用范围已经闭合。机器入口是`config/common/canonical-ruler-pool.json`；综合统计与总排名只能读取`COMPOSITE_READY`对象。
- 正式机器结果只位于 `docs/评分结算/**`，入口由 `config/project.yml` 列出。
- `docs/公共成果/`、`docs/治理/`、`docs/史料通读产物/` 只因仍被评分 lineage 引用而保留。
- 第一项和第三项保留确定性重建逻辑；第二、第四、第五项当前以已验收正式快照为准。
- 模型抽取、浏览器检索、source-cache、I5B shadow、单皇帝/单朝代运行器、PostgreSQL 服务和部署链均已退役。
- 数据库写入和生产部署不在当前执行范围；跨项综合排名只由正式命令读取前四项正式结果与`COMPOSITE_READY`对象确定性生成。

## 工作树边界

- 修改前后运行 `git -c core.quotepath=false status --short`，不覆盖用户改动，不默认 stash、clean、reset 或改写历史。
- Git 是唯一历史载体；不保留旧结果、阶段报告、失败运行、checkpoint、兼容字段或状态镜像。
- `tmp/**` 与 `.tmp/**` 只放可丢弃的验证产物，未经允许不得迁入正式目录。
- 新文件必须直接服务于评分正确性、归责、去重、证据追溯、正式结果读取或验证。
- 删除前核对被引用关系；仍被正式 JSON、规则合同或结算生成器引用的证据不得删除。

## 评分与证据

- 采用最小充分证据，但对象、行为、皇帝归责、结果方向和史源 lineage 必须严格。
- 同一成果或损害只在最符合其核心含义的项目结算一次。
- 第一项“不适用”不是零分；综合公式和各项边界以评分总则为准。
- 正式 JSON 是机器入口，Markdown 是同值阅读视图；两者不得分叉。
- 第二、第四、第五项没有当前生成器时，不得用临时脚本或旧 shadow 覆盖正式结果。

## 实现与验证

- 保留代码必须离线、确定性、零模型、零网络、零外部数据库。
- 只保留当前输入与当前输出，不新增 migration、worker、service、deployment 或兼容旧格式分支。
- 修改评分逻辑时运行聚焦测试；修改公共入口或大范围清理时运行全量测试。
- 最低验证包括：五项正式结算校验、JSON/YAML 解析、`compileall`、相关测试、`git diff --check`、删除后悬空引用检查。
- 结果不能只报告退出码；应报告五项覆盖、文件范围、删除数量和仍保留的限制。

### 禁止固化裁决快照的测试

- 禁止新增、恢复或维护把可变裁决结果写死为预期答案的测试代码，包括具体人物的分数、总分、排名、档位、档内位置、雷达值，以及固定的档位分布、材料数量、覆盖人数和历史整改数量。不得在每次合法改判后靠更新这些常量维持测试通过。
- 禁止复制当前正式结果、整份快照、哈希或人物裁决文案作为永久测试基准；不得把同类断言移入验证器、脚本或测试夹具规避本规则。不得让测试依赖已退役的结算入口或历史阶段产物。
- 测试只验证明确的合同约束和实现行为，如公式、边界条件、共享交班锚、归责与去重、证据引用、JSON/Markdown及上下游同值、只读性和输出确定性。人数、分布及材料覆盖应与当前规范入口或实际记录动态核对，不另存一套固定答案。
- 合同规定的权重、阈值、枚举，以及独立构造的最小合成用例可以使用固定预期值；不得以合成用例名义复制真实人物的当前裁决。校验失败必须区分实现错误、数据不一致和过期测试，不能为通过测试反改正确裁决，也不能删除有效校验掩盖真实问题。
- 局部改判只运行与本次改动直接相关的轻量校验，不默认运行无关分项测试；不修改代码的纯文档编辑只检查文本差异与格式。上述范围优先于本节通用最低验证清单，避免无关重建和重复验证。

## Windows 与编码

- PowerShell 一律使用 `pwsh.exe`；Python、pytest 和校验命令优先通过 `codex-win run --` 执行。
- 中文 Markdown、JSON、YAML 使用 UTF-8 no BOM。
- 不通过 PowerShell inline、管道或 here-string 传递大段中文、JSON 或 Markdown；使用 UTF-8 文件、仓库工具或 `apply_patch`。

## 默认忽略

除非任务明确点名，不主动读取或改写：

```text
tmp/
.tmp/
.cache/
logs/
.env
.codex/
```

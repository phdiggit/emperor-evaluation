# AGENTS.md

本仓库是“皇帝综合评价体系 V4”已结算工作区。当前目标是维护五大项评分的规则、证据、确定性结算逻辑和唯一正式结果，不再维护历史采集或服务运行平台。

## 优先级

1. 当前用户任务中的范围、禁止事项和显式批准；
2. 本文件；
3. `docs/项目总纲/皇帝综合评价体系评分标准.md`；
4. 分项规则与 `config/project.yml`；
5. 既有实现和生成物。

本文件补充项目差异；授权、Shell、技能选择和通用 Git 操作沿用全局约定。发生冲突时说明依据，按上位规则推进；仅在关键事实无法确定并阻塞交付时询问用户。

## 按任务读取

- 评分维护从 `config/project.yml` 定位相关入口、人物记录与合同章节；涉及总公式或跨项边界时再读总纲对应章节，证据有疑点时沿引用回源，不要求每次通读文档。
- 说明文案、展示或开发工具任务只读取相关文件与依赖。已读且未变化的资料复用；技能按任务触发，具体流程按需读取。
- 军事成本、第一项扣分、第三项合并或DA维护先按[军事成本合同导航](docs/分项规则/军事成本合同导航.md)选择主题；触发的准入、例外与去重条件必须读全，不默认连读全部关联合同。
- 文档先定位标题或字段，再读取完整相关章节；普通单次文本输出以不超过约3000 token为目标，过宽搜索先缩小范围，不能靠截断当作已读完整。大JSON由程序提取所需字段、统计和异常，不回传整份记录。已读内容只在变化、冲突或必要上下文缺失时补读。
- 合同只保留对象、公式、准入、归责、去重、必要例外及输出约束；同一规则定义一次，其他位置引用。删去重复解释、非规范人物示例和阶段过程，不另建摘要真源；压缩须核对必要条件、公式与引用不丢失。

## 当前事实

- 第一至第五项均有唯一当前结算入口；第四项全池语义复审已关闭，具体裁决与交班边界见正式入口及共享审计。后续局部改判须同步复核引用；结构校验本身不代替语义裁决。
- 独立人物画像采用逐轴结算；C1、C2、C3、C4、C5、M1、M2、M3、M4与M5十轴均已正式结算。画像不构成第六项，不进入综合总榜，不设画像总分或轴内排名；正式入口由`config/project.yml`的`profile_assessment`登记。
- 正式评价池人数、待补对象和就绪状态读取 `config/common/canonical-ruler-pool.json`；综合统计与总排名只能读取 `COMPOSITE_READY` 对象。
- 正式机器结果只位于 `docs/评分结算/**`，入口由 `config/project.yml` 列出。
- `docs/公共成果/`、`docs/治理/`、`docs/史料通读产物/` 只因仍被评分 lineage 引用而保留。
- 第一项和第三项保留确定性重建逻辑；第二、第五项以当前正式快照为准。第四项以逐人局部裁决维护当前快照，语义验收状态由其正式入口门禁声明，不能由重建阅读视图或榜单推定完成。
- 模型抽取、浏览器检索、source-cache、I5B shadow、单皇帝/单朝代运行器、PostgreSQL 服务和部署链均已退役。
- 数据库写入和生产部署不在当前执行范围；跨项综合排名只由正式命令读取前四项正式结果与`COMPOSITE_READY`对象确定性生成。

## 工作树边界

- 修改前后核对 Git 状态与任务范围；提交时只选择本任务文件或改动块，不混入用户已有改动。
- Git 保存历史；正式目录不维护过期结果、阶段报告、失败日志、checkpoint 或状态镜像。当前仍支撑裁决的史料、归责及去重依据不属于过期产物。
- 不再生成或维护历史过程审计、批次整改报告、阶段复裁包和验收状态镜像；历史回看使用 Git。临时排查输出放 `.tmp/`，当前裁决所需史料、归责与去重依据直接保留在正式记录或其证据来源中。
- 临时脚本、日志、诊断与草稿放 `.tmp/`；已授权的正式交付直接写目标位置，无需重复确认，不顺带提交临时产物。
- 新文件应服务于本次授权的评分、证据追溯、验证、展示、文档或开发支持任务。
- 删除前核对被引用关系；仍被正式 JSON、规则合同或结算生成器引用的证据不得删除。

## 评分与证据

- 采用最小充分证据，但对象、行为、皇帝归责、结果方向和史源 lineage 必须严格。
- 同一成果或损害只在最符合其核心含义的项目结算一次。
- 第一项“不适用”不是零分；综合公式和各项边界以评分总则为准。
- 权威输入以对应正式入口声明为准；第一项以 Markdown 结算源为准，其余按入口读取正式 JSON。派生结果与阅读视图必须同值，不得反向覆盖正确裁决。
- 第二、第四、第五项没有当前生成器时，不得用临时脚本或旧 shadow 覆盖正式结果。

## 实现与验证

- 评分、结算与其验证执行链必须离线、确定性、零模型、零网络、零外部数据库，不恢复已退役的服务、迁移或兼容旧格式流程。
- 展示制作按对应 SOP 执行，可使用已授权的 AI 制图；展示内容不得反向改写正式评分。
- 按下表选择相关已有检查，范围重叠时合并执行；不把所有检查叠成每次必跑清单。

| 改动类型 | 验证范围 |
| --- | --- |
| 不改变评分语义、机器输入或引用关系的说明文案 | 文本差异与格式 |
| 局部裁决、评分合同或正式结算源（含第一项 Markdown） | 受影响人物或组件的合同、源与阅读视图同值、实际受影响下游 |
| 单组件计算逻辑 | 聚焦测试、相关数据解析和 Python 编译检查 |
| 公共公式、加载机制、跨组件入口或大范围清理 | 按影响扩展集成检查；影响全链时运行五项正式结算校验与完整测试 |
| 展示内容或生成流程 | 正式数据来源、轴序与映射、受影响页面检查；相关展示测试按需运行 |
| 删除、移动或引用变更 | 检查受影响引用与入口；有计算影响时叠加对应组件检查 |

- 修改后检查任务文件的 `git diff --check`。检查命令见 `README.md`；局部维护优先使用组件校验，不默认重建全池。
- 复用输入、依赖、配置、环境和命令均未变化的有效验证结果；新改动、失败或未解决疑点才扩大或重跑。

### 禁止固化裁决快照的测试

- 禁止以历史审计、整改报告或阶段复裁包作为测试基准；不得要求这些文件存在、与当前重建内容一致，或在每次合法改判后同步旧报告才能通过。不得将同类门禁移入验证器、脚本或夹具；检查直接读取当前合同、正式记录及其有效来源。
- 禁止新增、恢复或维护把可变裁决结果写死为预期答案的测试代码，包括具体人物的分数、总分、排名、档位、档内位置、雷达值，以及固定的档位分布、材料数量、覆盖人数和历史整改数量。不得在每次合法改判后靠更新这些常量维持测试通过。
- 禁止复制当前正式结果、整份快照、哈希或人物裁决文案作为永久测试基准；不得把同类断言移入验证器、脚本或测试夹具规避本规则。不得让测试依赖已退役的结算入口或历史阶段产物。
- 测试只验证明确的合同约束和实现行为，如公式、边界条件、共享交班锚、归责与去重、证据引用、JSON/Markdown及上下游同值、只读性和输出确定性。人数、分布及材料覆盖应与当前规范入口或实际记录动态核对，不另存一套固定答案。
- 合同规定的权重、阈值、枚举，以及独立构造的最小合成用例可以使用固定预期值；不得以合成用例名义复制真实人物的当前裁决。校验失败必须区分实现错误、数据不一致和过期测试，不能为通过测试反改正确裁决，也不能删除有效校验掩盖真实问题。

## 完成与交付

- 授权交付物完成、受影响下游同步、相关验证通过、改动范围核对完毕即结束；不因无关既存问题或可选优化扩大任务、重复确认或提前停工。
- 报告实际改动、验证结论及限制；全池任务报告覆盖，删除任务报告删除与引用检查。确有阻塞时说明已完成部分、具体阻塞和未验证项，并继续不受阻的工作。
- 提交、推送与发布按用户任务授权执行；不把额外提交或仓库全绿作为普通局部任务的完成前提。

## 编码与临时目录

- 中文 Markdown、JSON、YAML 使用 UTF-8 no BOM；Shell 与复杂载荷处理遵循全局约定，`codex-win` 按实际需要使用。
- 默认不扫描 `tmp/`、`.tmp/`、`.cache/`、`logs/`、`.codex/`；任务依赖或排障所需时可定向读取，无需用户逐一路径点名。`.env` 等凭据文件继续遵守全局凭据保护约束。

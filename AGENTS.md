# AGENTS.md

## 项目工作原则

本仓库是“皇帝综合评价体系”工作区。执行任务时优先遵守用户在 Issue / PR 中给出的变更白名单与禁止事项。

默认工作方式：

1. 先读取 Issue / PR 任务说明，确认允许修改的文件白名单。
2. 修改前先用 `git diff --name-only` 确认当前工作区。
3. 修改后再次运行 `git diff --name-only`，白名单外文件必须还原。
4. 不要自动生成交接包、压缩包、总榜、排名或正式分数，除非用户明确要求。
5. 用户主要审规则、算法、边界和方法论；具体案例材料只作为规则压力测试材料。
6. 文件清理、归档、删除候选第一轮只写诊断报告或候选清单，不直接删改。
7. `exports/markdown_views/` 是导出视图层，不是事实源；未明确指定时不要批量重写旧导出。
8. `data/*_batches/` 是过渡批次层，确认唯一数据源前不得删除。
9. 大脚本治理优先小步重构加测试锁定，不在业务 PR 里顺手拆脚本。
10. 暂不引入外部数据库、缓存或中间件，除非专门架构 Issue 明确授权。
11. 任何 PR 都必须粘贴最终 `git diff --name-only`。
12. 开 PR 后默认直接置为 ready for review；除非 Issue 明确要求 draft，否则不要保持 draft。

## 默认不要读取或总结的目录

除非 Issue 明确要求，不要主动读取、总结或改写以下目录：

```text
exports/
logs/
tmp/
.cache/
.codex/
```

说明：

- `exports/` 通常是生成视图，除非任务明确要求新增或更新某个指定导出文件，否则不要批量重写。
- `logs/`、`tmp/`、`.cache/` 属于临时/缓存目录，不纳入工作总结。
- `.codex/` 属于本地代理/工具配置或运行痕迹，不纳入项目内容判断。

## 导出文件规则

不要运行会批量重写旧导出的命令，除非任务明确要求。

如果必须运行导出脚本：

1. 运行后立即检查 `git diff --name-only`。
2. 白名单外导出文件必须还原。
3. 不得因换行、编码或格式化导致旧导出视图进入 PR。

## 禁止事项

默认禁止：

- 修改旧索引、旧三人视图、旧净证据池、旧定档表，除非任务明确列入白名单。
- 输出正式档位、正式分数、内部试算分、排名、阶段总榜或总榜。
- 把补证、readiness、human review package 自动升级为正式结论。
- 把军功、政权安全、财政绩效、司法严酷等相邻项内容直接回填到当前子项。

## 提交前检查

提交或开 PR 前必须确认：

```bash
git diff --name-only
```

PR 说明中必须粘贴最终 changed files 列表。

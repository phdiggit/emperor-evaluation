[pytest]
testpaths = tests
markers =
    export_full: tests that run full or detail export profiles and may rewrite generated Markdown outputs
    integration: tests that cross multiple scripts, generated DBs, or real repository layout
    slow: long-running tests not required for every small PR
    snapshot: golden/snapshot-style output checks; keep small and explicit
    db: tests that build or inspect SQLite/database artifacts

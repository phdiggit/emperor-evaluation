# V4 遗留资产复核

本清单是 V3 冻结点进入 V4 后的人工适用性结论。状态只有：
`accepted_as_v4`、`needs_rewrite`、`legacy_reference_only`。

| 路径 | 状态 | V4 处理 |
| --- | --- | --- |
| `README.md` | `accepted_as_v4` | 已改写为 V4 入口，不继承 V3 运行态说明。 |
| `AGENTS.md` | `accepted_as_v4` | 已改写为 V4 边界与事实源规则。 |
| `docs/皇帝综合评价体系评分标准.md` | `accepted_as_v4` | 保留为最高层业务标准；后续版本变更必须专门审查。 |
| `docs/项目总纲/总规则.md` | `needs_rewrite` | 核心原则有效，但含 V3.2、G8/G9 和旧发布状态。 |
| `docs/证据规则/证据裁量总则.md` | `needs_rewrite` | 证据裁量原则可继承，旧自动结算链和数据对象需重写。 |
| `docs/证据规则/史料检索与回源工作流.md` | `needs_rewrite` | 史源分级可继承，旧表名与 V3 工作流不得直接成为 V4 约束。 |
| `docs/分项规则/第五项统治者政治素质/B用人与授权.md` | `legacy_reference_only` | 仅作规则语义与反例参考，不继承 V3 公式、表结构或发布结论。 |
| `config/君主别名.yml` | `needs_rewrite` | 作为候选词表保留，使用前需按三人试点裁剪并校验歧义。 |
| `config/所有君主.yml` | `legacy_reference_only` | 仅作未来对象范围参考，不进入当前三人试点。 |

## 明确不携带

`scripts/`、`tests/`、`db/`、`exports/`、`logs/`、`tmp/`、
`archive/`、`.codex/`、旧 migrations、旧 registry、旧 source pack
业务数据、旧 claim/material/binding/judgment/score 数据、旧 systemd worker
文件和旧运行说明均不属于 V4 基线。

V3 数据库与审计产物留在 V3 环境，只可用于回归样本、负面案例、人工对照和
shadow comparison；不得导入为 V4 正式业务数据。

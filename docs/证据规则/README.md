# 证据规则

## 本目录负责什么

本目录负责史料如何进入证据链、证据如何入分前裁量、负证如何触发、对象锚点如何校准，以及跨项目可复用的证据强度和裁判边界。

## 本目录不负责什么

本目录不保存 JSONL 字段事实、不承载 SQLite 生成库说明、不写 Markdown 展示格式，也不为单个复杂子项增开多份分项规则文档。

## 与相邻目录的边界

- 字段、配置、稳定 ID 和批次生命周期，进入 `数据结构与生成库/`。
- 子项专用计入边界、相邻项切分和专用触发点，进入 `分项规则/`。
- 人工阅读导出和 GitHub 协作，进入 `展示与协作/`。

## 当前索引

| 文件 | 当前职责 | 状态 |
| --- | --- | --- |
| [`史料检索与回源工作流.md`](史料检索与回源工作流.md) | 管“材料如何进入证据链”：检索线索、史源主机分级、source document、source passage、上下文窗口、回源状态和证据链接。 | 有效入口 |
| [`证据裁量总则.md`](证据裁量总则.md) | 管“材料如何裁量”：证据强度、五轴、对象锚点、正负证净裁量、证据组、相邻项切分和人工裁判状态。 | 有效入口 |
| [`负证触发式裁判通用规则.md`](负证触发式裁判通用规则.md) | 只保留跨项目可复用的负证触发模块，供总则调用；不重复完整五轴和分项评分规则。 | 有效入口 |
| [`证据链总流程与文档索引.md`](证据链总流程与文档索引.md) | 原流程索引，内容已吸收到本 README、检索工作流和裁量总则。 | 退役说明 |
| [`证据强度四级与五轴裁量规则.md`](证据强度四级与五轴裁量规则.md) | 原四级强度与五轴量化说明，核心内容已吸收到裁量总则。 | 退役说明 |

对象锚点内容本轮仍保留在 [`证据裁量总则.md`](证据裁量总则.md) 中；后续如需拆分，应另开专门 PR，不在本轮重复新造长文。

## 职责重叠收束

本轮收束后的主入口为“检索与回源”“裁量总则”“负证触发”三层：

1. `史料检索与回源工作流.md` 到原子证据卡为止，只处理线索、回源、上下文和 source passage 关系，不定档、不定分。
2. `证据裁量总则.md` 从原子证据卡开始，处理候选强度、五轴、对象锚点、证据组和人工裁判门槛，不替代分项标尺。
3. `负证触发式裁判通用规则.md` 只提供高风险负证的触发模块；单个子项的专用计入边界仍进入 `分项规则/`。

`证据链总流程与文档索引.md` 与 `证据强度四级与五轴裁量规则.md` 不再作为阅读入口，保留短退役说明用于追溯旧引用。

## 后续新增文件限制

新增文件必须是跨项目、跨分项可复用的证据规则。若内容只服务某一子项，应进入 `分项规则/` 且遵守“每个子项目最多一个分项规则文档”。

# G2.6K0 史源切片开发资格

状态：`g2_core_closed_g3a_authorized_relation_deferred`

日期：2026-07-13

契约基线：`source-cache-contract-v2` + `assertion-extraction-contract-v2`

## 1. 结论

G2.6K0 是开放开发资格，不是新 blind holdout。G2.6I/J 已开封为回归集。S1/S2 与 S3 Episode 已达到 shadow
implementation 门槛，G2-Core 调试到此结束。独立 Relation review、Relation semantic policy v2 和 Relation Gold
ontology audit 已完成，但修订 Gold 下的 S4 strict precision/recall 仍未达标；G2-Relation 因此转入独立 G3R，
不再阻断仅持久化 Episode Core 的 G3A/G3B。新 blind holdout、正式 Relation、规则投影、评分和生产写入继续阻断。

## 2. 分层门禁

| 层 | 对象 | 最低资格 | 失败动作 |
| --- | --- | --- | --- |
| S1 | SourcePassage | v2 字段 100%；导航噪声 0；linked lineage 100% | `STOP_BEFORE_BOUNDARY` |
| S2 | Assertion | atomic support ≥ 75%；context-only ≤ 25%；所有 passage 有支持分类 | `STOP_BEFORE_BOUNDARY` |
| S3 | HistoricalEpisode | recall ≥ 85%；precision ≥ 90%；lineage 100%；灾难性/跨皇帝错误 0 | `BOUNDARY_QUALITY_BELOW_MINIMUM` |
| S4 | HistoricalEpisodeRelation | Gold relation ≥ 4；strict precision ≥ 90%；strict recall ≥ 85% | `RELATION_QUALITY_BELOW_MINIMUM` |
| S5 | RuleEvidenceUnit | Gold rule evidence unit ≥ 4 | `COVERAGE_INELIGIBLE_FOR_RULE` |

S1/S2 失败时，下游 reviewer 启动数必须为 0。Historical Gold 中 relation 为 0 时，不得再报告关系 recall；Rule Gold
unit 为 0 时，不得生成 release score。candidate 数量形成的 recall 上限低于阈值时，应在 Rule Gold 之前停止。

## 3. Source Cache v2

v2 passage 必须保留：

- document content version；
- section identity 与 heading；
- 文档内半开 span；
- `atomic | context | navigation_noise` kind；
- linked passage relation；
- overlap group；
- window policy version。

稳定 passage 身份不依赖摘要、Gold code、候选边界或数据库 ID。refresh 产生新 content version，不覆盖旧片段。

## 4. 既有开发集与 J 组恢复结果

确定性命令：

```powershell
$env:PYTHONPATH = "src"
python -m emperor_v4.eval source-development-qualification `
  --input eval/g2_6i_graph_blind_v1/input.json `
  --input eval/g2_6j_graph_blind_v1/input.json `
  --output eval/g2_6k0_source_development_qualification.json
```

当前结果：

| 开放开发集 | Source v2 coverage | atomic support | context-only | 导航噪声 | 判定 |
| --- | ---: | ---: | ---: | ---: | --- |
| G2.6I | 0/59 | 0/59 | 0/59 | 0 | S1/S2 阻断；旧输入未做 PassageSupport 分类 |
| G2.6J | 0/38 | 7/38 | 31/38 | 2 | S1/S2 阻断 |

旧输入仍作为失败基线保留，不原地改写。对 J 组另建开放开发输入：

```powershell
$env:PYTHONPATH = "src"
python -m emperor_v4.eval source-development-materialize `
  --manifest eval/g2_6k0_source_recovery_v1.yml `
  --claim-snapshot eval/g2_6j_graph_blind_v1/source_claim_snapshot.json `
  --snapshot-dir .tmp/g2_6k0_wikisource `
  --output eval/g2_6k0_g2_6j_source_v2_development/input.json

python -m emperor_v4.eval source-development-qualification `
  --input eval/g2_6k0_g2_6j_source_v2_development/input.json `
  --output eval/g2_6k0_g2_6j_source_v2_development/qualification.json
```

J 组恢复输入使用 7 个有 revision 和 content hash 的公开史源页面，经确定性 anchor、section span 和 window
policy 生成 18 条 passage-scoped Assertion。I 组恢复输入使用《史記》三卷和《清史稿》两卷，将原 59 条 Assertion
重绑定到 27 条唯一 SourcePassage；两组重复语义 claim 复用相同文档 span，不为相同史料制造新 ID。两条生成链均不读取
Historical/Rule Gold、episode code、expected participant、must-merge、must-not-merge 或 acceptance decision。

| 开放开发输入 | Source v2 coverage | atomic support | context-only | 导航噪声 | linked lineage | 判定 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| G2.6J source v2 | 18/18 | 18/18 | 0/18 | 0 | 100% | S1/S2 通过 |
| G2.6I source v2 | 27/27 | 27/27；59 assertions 已分类 | 0/27 | 0 | 100% | S1/S2 通过 |

两份正式取源审计合计记录 12 次页面请求、0 次数据库写入、未启动 V3 runtime。V3 cache 与只读数据库探查没有恢复出
足以构造 v2 passage 的完整来源身份和正文，因此没有把短 preview 伪装成正式史源。

G2.6K0 的 S1/S2 总结论为 `development_sets_qualified_for_boundary`。后续 Boundary/Historical/Rule Gold 均只属于已开封开发集；
其结果不得改写或替代既有 blind 成绩。

## 5. S3/S4 开放开发结果

Boundary policy v2.10 将共享 participant/object surface 的跨 focal 命题放入同一连通 ReviewUnit，并允许在同责任族、
同规范化时间且共享对象或 passage 时，把互补 action/focal-role 冻结为同一原子状态转换。Gold、candidate 和 score 均未向
Boundary reviewer 暴露；但由于政策由已开封开发误差驱动，本轮只能标记为 `post_gold_open_development`。

| 开放开发集 | Episode recall | Episode precision | 普通 wrong merge/split | catastrophic | 跨皇帝 | lineage | S3 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| G2.6I source v2 | 100% | 100% | 0 / 0 | 0 | 0 | 100% | 通过 |
| G2.6J source v2 | 87.5% | 93.33% | 1 / 0 | 0 | 0 | 100% | 通过 |

S4 不以“Gold 中存在关系”冒充关系质量。Boundary 内部关系的初始结果为 I 20.83%/21.74%、J 40%/25%。
独立跨 ReviewUnit Relation review v1 虽补齐了比较范围，但类型语义未定义，仍然失败。policy v2 随后固定了类型优先级、
方向、最小图和 `unresolved` 边界，254 个 pair 被逐项重审，I/J 分别提出 22/5 条关系，0 未决。

在不读取 candidate、review 或 score 的独立 Gold ontology audit 中，I 原 23 条关系的结论为 13 条原样支持、2 条改类型、
8 条不是直接关系；J 的 8 条为 5 条原样支持、3 条改类型。机械修订后的 Relation Gold v2 含 I 15 条、J 8 条，且
明确标记为 open-development，不属于新 blind 资格证据。对 v2 Gold 的唯一重评分是：

| 开放开发集 | Gold Relation | strict precision | strict recall | S4 |
| --- | ---: | ---: | ---: | --- |
| G2.6I source v2 | 15 | 50% | 73.33% | `RELATION_QUALITY_BELOW_MINIMUM` |
| G2.6J source v2 | 8 | 40% | 25% | `RELATION_QUALITY_BELOW_MINIMUM` |

跨 ReviewUnit 不可比较和旧 Gold 本体不一致两个问题均已关闭。剩余阻断是 relation-specific evidence 与 proposal agreement：
I 仍有 4 条 Gold 漏失和 11 条额外候选；J 有 6 条 Gold 漏失、3 条额外候选和 1 个类型错配。不得再按开发 Gold 逐 pair
启动 policy v3 追分。2026-07-13 的小型 relation evidence/双审一致性实验及其结论见
`21-G2终止与G3A核心Shadow决策.md`；无论实验是否达到可学习性阈值，本轮都不再重审 254 对、不创建 policy v3，
也不启动新 blind。

旧 Rule Gold 建立在原 Relation Gold 上；I 的 Relation Gold v2 删除了 8 条边并修改 2 个类型，因此旧 Rule Gold 不再具备
当前 ontology 下的资格效力。机械 orchestrator 在 S4 失败后仍将 S5 设为 `null`。只有独立 G3R 达到原 S4 门槛后，
才能重新冻结 Rule Gold 并开启 G3C。

## 6. 开发与最终资格分离

开发期间只使用：

- G2.6I/J 及既有失败集；
- 8 场景离线 protocol smoke；
- 通用契约与不变量测试。

不得通过人物名、事件 code、expected participant 或逐事件 if/else 修补回归集。S1—S5 全部通过后，才冻结一次最终
qualified holdout；Kernel 执行前仍禁止访问 episode code、Gold boundary、must-merge、must-not-merge 和 Gold linkage。

## 7. G2 结束与 G3 后续拆分

当前分层状态为：

| 轨道 | 状态 | 边界 |
| --- | --- | --- |
| G2-Core / S1—S3 | `passed_for_shadow_implementation` | 只授权 Episode Core shadow |
| G2-Relation / S4 | `deferred_not_qualified` | 原 90%/85% 阈值不变 |
| G2-Rule / S5 | `blocked_by_relation_track` | 旧 Rule Gold stale |
| G3A-Core-Registry | `passed_shadow_registry` | 隔离 V4 shadow store 已验证 |
| G3B-Core-Runner | `authorized_for_implementation` | 同步、事务化、幂等、局部失效 |
| G3R-Relation | `experimental_independent_track` | 不阻断 G3A/G3B |
| G3C-Formal-Projection | `blocked` | 不得规则投影、判断或评分 |

G3A 只包含 Source、Assertion、HistoricalEpisode、EpisodeVersion、Participant、AssertionDisposition、review artifact 与
boundary cache。Relation 输出最多保存为 proposal/artifact，不进入 accepted core fact。G3B 只验证零新增重跑、语义/证据版本分离、
局部失效和零模型/零业务写入不变量。G3R 单独解决候选 blocking、双审一致性、粗细类型和图不变量；未证明同步路径需要前不引入
worker/outbox。

本授权不允许连接 V3 数据库、恢复 V3 worker、执行生产部署、写正式评分或排名，也不等于已授权具体 PostgreSQL 实例或凭据。

# G2.6J 盲测结果与史源切片阻断

状态：`failed_closed_after_first_blind_score`

日期：2026-07-13

契约基线：`assertion-extraction-contract-v2` + `episode-boundary-review-v2.8`

## 1. 结论

G2.6J 的 passage-scoped Assertion 契约和 fail-closed 边界生效，但本批不能放行 G3。

本批只证明：在独立 PassageSupport 审核过滤掉过度切碎的证据后，Kernel 对剩余 7 个单 Assertion Episode 能保持边界、lineage 和跨皇帝隔离。它没有证明多证据 merge/split、EpisodeRelation 或 `appointment_delegation` rule projection 达标。

最终状态：

```text
G2.6J: failed_closed_after_first_blind_score
episode_singleton_boundary: component_pass
source_segmentation: failed
episode_relation: failed
rule_projection_coverage: not_qualified
G3: blocked
```

## 2. 隔离与输入

新 holdout 使用杨广、胡亥两个此前 G2.6E—I 未使用的评估上下文。源数据来自 V3 只读快照：

- 18 个 active claim；
- 38 个 source passage；
- 14 个 CanonicalPerson candidate；
- 0 次网络请求；
- 0 次数据库写入；
- 未提交私网地址。

Kernel 输入不含 episode code、Gold boundary、must-merge、must-not-merge、expected participant、acceptance decision、Gold linkage 或 candidate key。

四层冻结顺序为：

```text
source snapshot
→ PassageSupport review
→ strict input / review plan
→ Boundary review + Historical Gold
→ Rule Gold
→ candidate materialization
→ Gold score
```

Boundary reviewer 未读取 Historical/Rule Gold、candidate 或 score；Historical Gold reviewer 未读取 review plan、Boundary review、candidate 或 score；Rule Gold reviewer只在 Historical Gold 冻结后启动。

## 3. Candidate 前的通用契约修复

所有修复都发生在 Boundary 判断、Historical/Rule Gold、candidate 和 score 之前，且没有读取任何 Gold 结果：

1. proposition 聚类改为遵守 claim-local `assertion_semantic_key`，旧 claim key 不再把多个 atomic component 拼回去；
2. `context_only` Assertion 在领域层禁止进入 Episode core；
3. 全 `context_only` review unit 允许以零 Episode、全 unresolved/excluded 的方式关闭；
4. proposition identity 纳入 claim-local PassageSupport key，避免语义表面相同的独立原子分量产生重复 proposition code；
5. BoundaryReview v2.8 的冻结状态与隔离/hash 字段和 graph materializer 对齐。

这些是通用 contract/schema 修复，不含人物名、事件 code 或预期边界硬编码。

## 4. PassageSupport 冻结结果

独立审核逐 passage 检查原文是否自身支持主体与动作：

| support mode | 数量 | 处置 |
| --- | ---: | --- |
| `atomic_component` | 7 | 可进入 Episode core 审查 |
| `context_only` | 31 | 不得进入 Episode core |
| `equivalent_evidence` | 0 | 本批没有可证明完全同义的多来源证据 |
| `single_passage` | 0 | 本批 claim 均有多 passage lineage |

31 个 context-only passage 多为“竟坐诛”“出为河池郡守”“吞药自杀”一类缺明示主体或动作的短片，其中 2 段是页面导航噪声。不得借旧 claim 关联反向补齐主体。

因此本批首要失败层已经定位为：

```text
source passage segmentation
```

而不是让 Boundary 模型“更敢合并”。

## 5. 独立冻结结果

### 5.1 Boundary review

- 16/16 review units；
- 38/38 Assertion 主处置；
- 7 个 Episode group；
- 2 个 proposed relation；
- 7 个 `core_of_episode`，全部来自 atomic component；
- 4 个 `context_for_episode`；
- 25 个 unresolved；
- 2 个 excluded；
- context-only 进入 core：0。

### 5.2 Historical Gold

- 7 个 Gold Episode；
- 0 个 Gold Relation；
- 10 个跨皇帝 catastrophic forbidden pair；
- 38/38 Gold assertion disposition；
- Gold core passage lineage：100%。

### 5.3 Rule Gold

7 个 Gold Episode 均未形成完整的统治者任用/授权—对象—职责—反馈链，因此：

- eligible Episode：0；
- Gold rule evidence unit：0；
- 重复消费：0。

不得把司法处置、进言、恐惧状态或缺责任主体的 partial Episode 强行投影为 `appointment_delegation`。

## 6. 首次盲测指标

| 指标 | 结果 | 判定 |
| --- | ---: | --- |
| exact episode recall | 1.0000 | 7/7 singleton Gold Episode 命中 |
| exact candidate precision | 1.0000 | 7/7 candidate 精确命中 |
| pairwise same-episode precision | N/A | 没有同 Episode assertion pair |
| pairwise same-episode recall | N/A | 没有同 Episode assertion pair |
| wrong merge | 0 | 通过 |
| wrong split | 0 | 通过 |
| catastrophic wrong merge | 0 | 通过 |
| cross-ruler contamination | 0 | 通过 |
| passage lineage | 1.0000 | 通过 |
| assertion disposition coverage | 1.0000 | 通过 |
| strict relation precision | 0.0000 | 2 条 proposed relation 均无 Gold 对应 |
| strict relation recall | N/A | Gold Relation 为 0 |
| exact rule-unit precision / recall | N/A | candidate 与 Gold unit 均为 0 |
| release gate | `false` | 未通过 |
| G3 authorized | `false` | 禁止 |

Episode recall/precision 的 100% 不能解释为 G2.6 正式通过。7 个 Gold Episode 都只有一个 core Assertion，因此这两个指标只验证了 singleton preservation；merge/split 的 pairwise 指标没有样本，关系精度为 0，规则层没有合格样本。

## 7. 运行约束

无变化重跑结果：

- candidate graph canonical fingerprint 一致；
- rule candidate fingerprint 一致；
- 模型调用：0；
- 数据库写入：0；
- 网络请求：0；
- 单条 Assertion 变化只改变 1 个 review unit；
- 影响其他 review unit：0。

V3 worker 保持停止，未发生 V3/V4 生产写入或 Wiki 流量竞争。

## 8. 失败归因

| 现象 | 层级 | 结论 |
| --- | --- | --- |
| 31/38 passage 缺主体或动作 | source segmentation | 首要阻断 |
| 页面导航进入 evidence | source cache cleanliness | 需在缓存层过滤 |
| 7 个 Episode 全是 singleton | evaluation coverage | 无法验证 merge/split |
| Boundary 提出 2 条关系、Gold 为 0 | relation reconciliation | 关系推断过度 |
| 0 个 appointment/delegation Gold unit | holdout rule coverage | 不具 G3 放行资格 |

## 9. 下一步：G2.6K

G2.6J 永久冻结为失败开发集，不得修改本批 input、PassageSupport、Boundary、Gold 或 score 后重新标记为 blind pass。

进入新 holdout 前先修 source segmentation：

1. section-aware passage 必须保留明示主体、动作和必要前后指代；
2. 任命/授权与结果相隔较远时，使用带 lineage 的 linked passage window，不复制完整 claim payload；
3. 页面导航、目录和纯站点噪声在 source cache 层排除；
4. refresh 生成新 content version，不覆盖旧片段；
5. PassageSupport 继续独立冻结，缺 identity+action 的证据继续 fail closed。

G2.6K 必须使用未见人物链和未见 passage，并在 Gold 打开前满足输入资格：

- 至少 12 个可独立核验 atomic component；
- 导航噪声为 0；
- context-only 比例不高于 25%；
- 至少覆盖两位皇帝；
- 源 claim 选择中明确包含任用/授权、职责和结果链，但不预置 Episode boundary；
- Historical Gold 若仍没有可测 relation 或 `appointment_delegation` unit，则直接判定 coverage-ineligible，不用于 G3 放行。

G2.6K 达标前，不进入 PostgreSQL Episode Registry、状态机或正式规则投影。
